[package]
name = "aleatoric"
version = "0.1.0"
edition = "2021"
description = "Aleatoric (random) music generator with sawtooth synthesis"
license = "Apache-2.0"

[dependencies]
hound = "3.5"
rand = "0.8"
rodio = "0.17"

[profile.release]
opt-level = 3
